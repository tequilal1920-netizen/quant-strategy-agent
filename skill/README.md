# Skill 目录

本目录保存三类 Skill：

1. `现有Codex技能`：指向 `agent/skill`，保留旧兼容技能。
2. `统一AI模型技能`：指向 `agent/ai-models`，每个模型包内都有 `SKILL.md` 和查询脚本。
3. 本次新增的外层中文 Skill：绘图、SHH、GitHub，以及六个一级模型框架 Skill。

所有 Skill 只写公开调用方法、模型框架和工作纪律；不写账号、token、密码、私密数据库路径或授权明细。
