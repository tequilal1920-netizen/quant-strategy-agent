# 外层整理说明

`G:\中信建投` 的中文结构用于日常浏览和交接；`agent/` 是实际 Git 仓库和正式运行源码。外层目录中的大部分模型入口是目录连接，目的是让“数据看板、资产配置、行业风格、因子实验室、技术分析、组合优化、数据库、公网、environment、mcp、skill”按用户视角排布，同时保持唯一正式源码不分叉。

不能上传 GitHub 的内容：

- `reference/SOP` 中的 Word/Excel 文档；
- 本地数据库、缓存、授权数据；
- 私密环境变量、账号、token、cookie；
- 历史归档中的旧发布包和中间输出。

可上传 GitHub 的内容：

- `agent/` 内正式源码；
- `agent/ai-models` 和 `agent/skill` 的公开模型 Skill；
- `agent/mcp` 的公开 MCP 骨架；
- `agent/environment/workspace_layout` 的结构说明。

## 2026-08-28 验收记录

- `reference/SOP` 已包含用户指定的 SOP Word/Excel 文档；根目录仍有 `agent.docx` 与较新的 `final.docx`，当前被本机进程占用，不能在不关闭文档的情况下安全移动。`reference/SOP/final.docx` 已保留旧副本，待文件释放后应以根目录较新版本覆盖 SOP 版本并归档旧副本。
- `reference/历史归档/agent未跟踪中间文件归档_20260828` 保存从正式源码树移出的 139 个未跟踪迭代脚本与部署热修复脚本。正式源码树只保留被当前后端、测试或公开说明引用的最终入口文件。
- `数据库/数据字典.xlsx` 已生成，内容为数据库文件、表字段和增量脚本索引，不含业务数据行；公开 GitHub 只同步说明，不同步本地数据库与私密数据。
